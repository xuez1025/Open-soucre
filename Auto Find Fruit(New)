----------------------------------------
--服務
----------------------------------------
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")

local LocalPlayer = Players.LocalPlayer

----------------------------------------
-- 防重複啟用
----------------------------------------
if getgenv().AutoFindFruitLoaded then
    local StarterGui = game:GetService("StarterGui")
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = "Auto Find Fruit",
            Text = "腳本已在運行中，請勿重複執行！",
            Duration = 3
        })
    end)
    return
end

getgenv().AutoFindFruitLoaded = true

--------------------------
-- WindUI 載入
--------------------------
local WindUI = loadstring(request({
    Url = "https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"
}).Body)()

local Window = WindUI:CreateWindow({
    Title         = "Auto Find Fruit",
    Icon          = "apple",
    Author        = "by xuez1025",
    Size          = UDim2.fromOffset(600, 400),
    Transparent   = true,
    Theme         = "Dark",
    User = {
        Enabled   = true,
        Anonymous = false,
        Callback  = function()
            local LocalPlayer = game.Players.LocalPlayer
            WindUI:Notify({ 
                Title = LocalPlayer.Name, 
                Content = "ID: " .. LocalPlayer.UserId, 
                Duration = 3 
            })
        end
    },
    Acrylic       = false,
    HideSearchBar = false,
    SideBarWidth  = 200,
    OpenButton = {
        Title           = "Auto Find Fruit",
        CornerRadius    = UDim.new(1, 0),
        StrokeThickness = 3,
        Enabled         = true,
        OnlyMobile      = false,
        Draggable       = true,
        OnlyIcon        = false,
        Color = ColorSequence.new(Color3.fromHex("#FF512F"), Color3.fromHex("#DD2476"))
    }
})

--UI被關閉可重複執行
Window:OnDestroy(function()
    getgenv().AutoFindFruitLoaded = nil
end)

----------------------------------------
--變數
----------------------------------------
--select Team
_G.SelectTeam = "Marines"

--ESP Fruit
_G.FruitESP = false

--Auto Store Fruit
_G.AutoStoreFruit = false

--自動抽果
_G.AutoRollFruit = false

--Auto Tween Fruit
_G.Tweenfruit = false
_G.ServerHopping = false
_G.HopDelayTime = 3
_G.TELEPORT_COOLDOWN = 1
_G.FruitFlySpeed = 300

_G.LastTeleportTime = 0
_G.IsMovingToFruit = false
_G.LastFruitTime = tick()
_G.ForceHopTriggered = false
_G.HopTargetJobId = nil
_G.HopConfirmConn = nil
_G.ScriptReady = false

_G.Fruit_LinearVelocity = nil
_G.Fruit_AntiGravity = nil
_G.Fruit_Attachment = nil
_G.Fruit_TeleportPauseUntil = 0
_G.Fruit_BlockedEntrances = {}
_G.Fruit_LastTeleportTicks = {}
_G.Fruit_TeleportFails = {}

local PlaceId = game.PlaceId
local JobId = game.JobId
local ServerBrowserRemote = ReplicatedStorage:FindFirstChild("__ServerBrowser")

local Fruit_OrbitOffset = nil
local Fruit_NextOrbitTime = 0

--自動執行
local queue_on_teleport = queue_on_teleport or (syn and syn.queue_on_teleport)

if isfile and isfile("Hub.file") then
    local success, content = pcall(function() return readfile("Hub.file") end)
    _G.AutoExecute = (success and content == "true")
else
    _G.AutoExecute = false
end

local ScriptLoadstring = [[if isfile("Hub.file") then
    if readfile("Hub.file") == "true" then
        loadstring(game:HttpGet("https://raw.githubusercontent.com/xuez1025/Open-soucre/refs/heads/main/Auto%20Find%20Fruit(New)"))()
    end
end]]

--快捷鍵
_G.CurrentKey = "G"
_G.KeyDisabled = false

----------------------------------------
-- 配置自動保存
----------------------------------------
local ConfigFolder = "AutoFindFruit_Config"
local ConfigFile = ConfigFolder .. "/settings.json"
local ConfigReady = false
local ConfigSaving = false
local ConfigDirty = false

local ConfigKeys = {
    "SelectTeam",
    "FruitESP",
    "AutoStoreFruit",
    "AutoRollFruit",
    "Tweenfruit",
    "HopDelayTime",
    "AutoExecute",
    "CurrentKey",
    "KeyDisabled"
}

local function CopyConfigValue(value)
    if type(value) ~= "table" then
        return value
    end
    local copy = {}
    for key, item in pairs(value) do
        copy[key] = CopyConfigValue(item)
    end
    return copy
end

local function CollectConfig()
    local data = {}
    for _, key in ipairs(ConfigKeys) do
        if _G[key] ~= nil then
            data[key] = CopyConfigValue(_G[key])
        end
    end
    return data
end

local function ApplyConfig(data)
    if type(data) ~= "table" then return end
    for _, key in ipairs(ConfigKeys) do
        if data[key] ~= nil then
            _G[key] = CopyConfigValue(data[key])
        end
    end
end

local function SaveConfig(force)
    if ConfigSaving or (not force and not ConfigDirty) or typeof(writefile) ~= "function" then
        return false
    end
    ConfigSaving = true
    local ok = pcall(function()
        if typeof(isfolder) == "function" and typeof(makefolder) == "function" and not isfolder(ConfigFolder) then
            makefolder(ConfigFolder)
        end
        writefile(ConfigFile, HttpService:JSONEncode(CollectConfig()))
    end)
    ConfigSaving = false
    if ok then ConfigDirty = false end
    return ok
end

local function LoadConfig()
    if typeof(readfile) ~= "function" or typeof(isfile) ~= "function" or not isfile(ConfigFile) then
        return false
    end
    local ok, data = pcall(function()
        return HttpService:JSONDecode(readfile(ConfigFile))
    end)
    if ok and type(data) == "table" then
        ApplyConfig(data)
        return true
    end
    return false
end

local InitialConfig = CollectConfig()
LoadConfig()
ConfigReady = true

task.spawn(function()
    local lastConfig = nil
    while task.wait(1) do
        if ConfigReady then
            local ok, currentConfig = pcall(function()
                return HttpService:JSONEncode(CollectConfig())
            end)
            if ok and currentConfig ~= lastConfig then
                lastConfig = currentConfig
                ConfigDirty = true
            end
            if ConfigDirty then SaveConfig(true) end
        end
    end
end)

--------------------------
-- 等待遊戲與角色載入
--------------------------
-- 等待遊戲完全載入
local function WaitForGameLoaded()
    if not game:IsLoaded() then
        game.Loaded:Wait()
    end
    -- 確保 Remote 也已載入
    pcall(function()
        ReplicatedStorage:WaitForChild("Remotes", 30)
    end)
    task.wait(1)
end

-- 等待角色完全載入（含核心部件）
local function WaitForCharacterReady()
    local char = LocalPlayer.Character
    if not char then
        char = LocalPlayer.CharacterAdded:Wait()
    end
    pcall(function()
        char:WaitForChild("HumanoidRootPart", 30)
        char:WaitForChild("Humanoid", 30)
    end)
    -- 選完隊伍後角色會重生，多等一會確保新角色完全載入
    task.wait(3)
end

--------------------------
-- 功能核心邏輯
--------------------------
--select Team
local function SelectTeam()
    local lp = game:GetService("Players").LocalPlayer
    if not lp.Team or lp.Team.Name ~= _G.SelectTeam then
        local args = {
            "SetTeam2",
            _G.SelectTeam
        }
        game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("CommF_"):InvokeServer(unpack(args))
    end
end

-- 載入順序：遊戲完全載入 → 選擇隊伍 → 角色完全載入 → 啟動其他功能
task.spawn(function()
    WaitForGameLoaded()
    SelectTeam()
    WaitForCharacterReady()
    _G.ScriptReady = true
    if WindUI then
        WindUI:Notify({
            Title = "載入完成",
            Content = "遊戲與角色已載入完成，功能已啟用",
            Duration = 3
        })
    end
end)

--ESP Fruit
local function round(num)
    return math.floor(num + 0.5)
end

local function clearEspForInstance(v)
    pcall(function()
        if v:FindFirstChild("Handle") and v.Handle:FindFirstChild("NameEsp") then
            v.Handle["NameEsp"]:Destroy()
        end
    end)
end

local function clearAllEsp()
    for _, v in pairs(Workspace:GetChildren()) do
        clearEspForInstance(v)
    end
end

RunService.RenderStepped:Connect(function()
    if not _G.FruitESP then return end
    
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("Head") then return end
    local headPos = character.Head.Position

    for _, v in pairs(Workspace:GetChildren()) do
        pcall(function()
            if string.find(v.Name, "Fruit") and v:FindFirstChild("Handle") then
                local handle = v.Handle
                local espName = "NameEsp"
                local dist = round((headPos - handle.Position).Magnitude / 3)
                local displayText = v.Name .. "\n[" .. dist .. "m]"

                local bill = handle:FindFirstChild(espName)
                if not bill then
                    bill = Instance.new("BillboardGui", handle)
                    bill.Name = espName
                    bill.ExtentsOffset = Vector3.new(0, 2, 0)
                    bill.Size = UDim2.new(0, 200, 0, 50)
                    bill.Adornee = handle
                    bill.AlwaysOnTop = true

                    local name = Instance.new("TextLabel", bill)
                    name.Font = Enum.Font.GothamBold
                    name.TextSize = 14
                    name.TextWrapped = true
                    name.Size = UDim2.new(1, 0, 1, 0)
                    name.TextYAlignment = Enum.TextYAlignment.Center
                    name.BackgroundTransparency = 1
                    name.TextStrokeTransparency = 0.2
                    name.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
                    name.TextColor3 = Color3.fromRGB(255, 230, 100)
                    name.Text = displayText
                else
                    bill.TextLabel.Text = displayText
                end
            end
        end)
    end
end)

Workspace.ChildRemoved:Connect(function(child)
    clearEspForInstance(child)
end)

--Auto Store Fruit
local FruitList = {
    ["Dragon-West"]     = "Dragon (West) Fruit",
    ["Dragon-East"]     = "Dragon (East) Fruit",
    ["Kitsune-Kitsune"] = "Kitsune Fruit",
    ["Tiger-Tiger"]     = "Tiger Fruit",
    ["Spirit-Spirit"]   = "Spirit Fruit",
    ["Control-Control"] = "Control Fruit",
    ["Venom-Venom"]     = "Venom Fruit",
    ["Shadow-Shadow"]   = "Shadow Fruit",
    ["Dough-Dough"]     = "Dough Fruit",
    ["T-Rex-T-Rex"]     = "T-Rex Fruit",
    ["Mammoth-Mammoth"] = "Mammoth Fruit",
    ["Gravity-Gravity"] = "Gravity Fruit",
    ["Blizzard-Blizzard"]= "Blizzard Fruit",
    ["Pain-Pain"]       = "Pain Fruit",
    ["Lightning-Lightning"] = "Lightning Fruit",
    ["Portal-Portal"]   = "Portal Fruit",
    ["Phoenix-Phoenix"] = "Phoenix Fruit",
    ["Gas-Gas"]         = "Gas Fruit",
    ["Yeti-Yeti"]       = "Yeti Fruit",
    ["Sound-Sound"]     = "Sound Fruit",
    ["Spider-Spider"]   = "Spider Fruit",
    ["Love-Love"]       = "Love Fruit",
    ["Buddha-Buddha"]   = "Buddha Fruit",
    ["Quake-Quake"]     = "Quake Fruit",
    ["Magma-Magma"]     = "Magma Fruit",
    ["Ghost-Ghost"]     = "Ghost Fruit",
    ["Creation-Creation"] = "Creation Fruit",
    ["Rubber-Rubber"]   = "Rubber Fruit",
    ["Light-Light"]     = "Light Fruit",
    ["Diamond-Diamond"] = "Diamond Fruit",
    ["Dark-Dark"]       = "Dark Fruit",
    ["Sand-Sand"]       = "Sand Fruit",
    ["Ice-Ice"]         = "Ice Fruit",
    ["Eagle-Eagle"]     = "Eagle Fruit",
    ["Flame-Flame"]     = "Flame Fruit",
    ["Spike-Spike"]     = "Spike Fruit",
    ["Smoke-Smoke"]     = "Smoke Fruit",
    ["Bomb-Bomb"]       = "Bomb Fruit",
    ["Spring-Spring"]   = "Spring Fruit",
    ["Blade-Blade"]     = "Blade Fruit",
    ["Spin-Spin"]       = "Spin Fruit",
    ["Rocket-Rocket"]   = "Rocket Fruit"
}

task.spawn(function()
    -- 等待角色完全載入後才開始執行
    while not _G.ScriptReady do task.wait(1) end
    while true do
        if _G.AutoStoreFruit then
            pcall(function()
                local character = LocalPlayer.Character
                local backpack = LocalPlayer:FindFirstChild("Backpack")
                
                for remoteName, toolName in pairs(FruitList) do
                    local fruitTool = nil
                    
                    if character then
                        fruitTool = character:FindFirstChild(toolName)
                    end
                    
                    if not fruitTool then
                        if backpack then
                            fruitTool = backpack:FindFirstChild(toolName)
                        end
                    end
                    
                    if fruitTool then
                        ReplicatedStorage.Remotes.CommF_:InvokeServer("StoreFruit", remoteName, fruitTool)
                    end
                end
            end)
        end
        task.wait(0.5)
    end
end)

-- 自動抽果
task.spawn(function()
    -- 等待角色完全載入後才開始執行
    while not _G.ScriptReady do task.wait(1) end
    task.wait(3)

    while true do
        if _G.AutoRollFruit then
            local response
            pcall(function()
                local args = {
                    "Cousin",
                    "DLCBoxData"
                }
                response = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("CommF_"):InvokeServer(unpack(args))
            end)
            
            if response then
                WindUI:Notify({ 
                    Title = "抽果結果", 
                    Content = tostring(response), 
                    Duration = 5 
                })
            end
            task.wait(600)
        else
            task.wait(1)
        end
    end
end)

--Auto Tween Fruit
local function ClearFruitFlightComponents()
    if _G.Fruit_LinearVelocity then _G.Fruit_LinearVelocity:Destroy(); _G.Fruit_LinearVelocity = nil end
    if _G.Fruit_AntiGravity then _G.Fruit_AntiGravity:Destroy(); _G.Fruit_AntiGravity = nil end
    if _G.Fruit_Attachment then _G.Fruit_Attachment:Destroy(); _G.Fruit_Attachment = nil end
end

local function SetupFruitFlightComponents(hrp)
    ClearFruitFlightComponents()

    local att = Instance.new("Attachment")
    att.Name = "XuHub_FruitFlightAtt"
    att.Parent = hrp
    _G.Fruit_Attachment = att

    local lv = Instance.new("LinearVelocity")
    lv.Name = "XuHub_FruitFlightVel"
    lv.MaxForce = math.huge
    lv.VelocityConstraintMode = Enum.VelocityConstraintMode.Vector
    lv.VectorVelocity = Vector3.new(0, 0, 0)
    lv.Attachment0 = att
    lv.Parent = hrp
    _G.Fruit_LinearVelocity = lv

    local totalMass = 0
    local char = hrp.Parent
    if char then
        for _, part in ipairs(char:GetDescendants()) do
            if part:IsA("BasePart") and not part.Massless then
                totalMass = totalMass + part:GetMass()
            end
        end
    end

    local vf = Instance.new("VectorForce")
    vf.Name = "XuHub_FruitAntiGrav"
    vf.ApplyAtCenterOfMass = true
    vf.Attachment0 = att
    vf.Force = Vector3.new(0, totalMass * workspace.Gravity, 0)
    vf.Parent = hrp
    _G.Fruit_AntiGravity = vf
end

local function SetNoCollide(char)
    if not char then return end
    for _, part in ipairs(char:GetDescendants()) do
        if part:IsA("BasePart") then
            part.CanCollide = false
        end
    end
end

local function SetCollide(char)
    if not char then return end
    for _, part in ipairs(char:GetDescendants()) do
        if part:IsA("BasePart") then
            local n = part.Name
            if n == "HumanoidRootPart" or n == "UpperTorso" or n == "LowerTorso" or n == "Head" then
                part.CanCollide = true
            end
        end
    end
end

local function doJump()
    local char = LocalPlayer.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if hum then hum.Jump = true end
end

local function GetCurrentWorldEntrances()
    local pool = {}
    local pid = game.PlaceId

    if table.find({2753915549, 85211729168715}, pid) then
        pool = {
            { Arg = Vector3.new(61163.85,  11.68,   1819.78), Dest = Vector3.new(61164,   5,    1820)  },
            { Arg = Vector3.new(3864.68,    6.73,  -1926.21), Dest = Vector3.new(3865,    5,   -1926)  },
            { Arg = Vector3.new(-4607.82,  874.39, -1667.55), Dest = Vector3.new(-4608,   873, -1668)  },
            { Arg = Vector3.new(-7894.61, 5547.14,  -380.29), Dest = Vector3.new(-7895,  5546,  -380)  },
        }
    elseif table.find({4442272183, 79091703265657}, pid) then
        pool = {
            { Arg = Vector3.new(2285,   15,   882),   Dest = Vector3.new(2285,   15,   882)  },
            { Arg = Vector3.new(-380,  350,   630),   Dest = Vector3.new(-380,  350,   630)  },
            { Arg = Vector3.new(924,   125, 32882),   Dest = Vector3.new(924,   125, 32882)  },
            { Arg = Vector3.new(-6491, 116,  -107),   Dest = Vector3.new(-6491, 116,  -107)  },
        }
    elseif table.find({7449423635, 100117331123089}, pid) then
        pool = {
            { Arg = Vector3.new(5678,    1013,  -312), Dest = Vector3.new(5678,    1013,  -312)  },
            { Arg = Vector3.new(-12551,  337,  -7507), Dest = Vector3.new(-12551,  337,  -7507)  },
            { Arg = Vector3.new(-5038,   315,  -3135), Dest = Vector3.new(-5038,   315,  -3135)  },
            { Arg = Vector3.new(-5098,   317,  -3179), Dest = Vector3.new(-16813,   58,    305)  },
        }
    end
    return pool
end

local function FlyToFruitLogic(targetPos)
    local char = LocalPlayer.Character
    local myHRP = char and char:FindFirstChild("HumanoidRootPart")
    if not myHRP then return end

    if os.clock() < _G.Fruit_TeleportPauseUntil then
        if _G.Fruit_LinearVelocity then
            _G.Fruit_LinearVelocity.VectorVelocity = Vector3.new(0, 0, 0)
        end
        myHRP.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
        return
    end

    local now = os.clock()
    if not Fruit_OrbitOffset or now >= Fruit_NextOrbitTime then
        local maxRadius = 20
        local dist = math.random(1, maxRadius)
        local d = Vector3.new(math.random() * 2 - 1, math.random() * 2 - 1, math.random() * 2 - 1)
        if d.Magnitude > 0 then d = d.Unit end
        Fruit_OrbitOffset = d * dist
        Fruit_NextOrbitTime = now + 0.05
    end
    local finalTarget = targetPos + Fruit_OrbitOffset

    local dir = finalTarget - myHRP.Position
    local distToFruit = (targetPos - myHRP.Position).Magnitude

    local directFlightTime = distToFruit / _G.FruitFlySpeed
    local bestEntrance = nil
    local bestTotalTime = directFlightTime
    local teleportPenalty = 1.5
    local pool = GetCurrentWorldEntrances()

    if distToFruit < 300 then pool = {} end

    for _, e in ipairs(pool) do
        if _G.Fruit_BlockedEntrances[e.Arg] then continue end

        local destDistToFruit = (e.Dest - targetPos).Magnitude
        local totalTimeViaPortal = (destDistToFruit / _G.FruitFlySpeed) + teleportPenalty

        if totalTimeViaPortal < bestTotalTime then
            if (distToFruit - destDistToFruit) > 150 then
                bestTotalTime = totalTimeViaPortal
                bestEntrance = e
            end
        end
    end

    if bestEntrance then
        local lastTime = _G.Fruit_LastTeleportTicks[bestEntrance.Arg] or 0
        if os.clock() - lastTime > 4 then
            _G.Fruit_LastTeleportTicks[bestEntrance.Arg] = os.clock()
            _G.Fruit_TeleportPauseUntil = os.clock() + 1.2

            local preDist = (myHRP.Position - bestEntrance.Dest).Magnitude

            if _G.Fruit_LinearVelocity then
                _G.Fruit_LinearVelocity.VectorVelocity = Vector3.new(0, 0, 0)
            end
            myHRP.AssemblyLinearVelocity = Vector3.new(0, 0, 0)

            task.spawn(function()
                pcall(function()
                    local commF = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("CommF_")
                    commF:InvokeServer("requestEntrance", bestEntrance.Arg)
                end)
            end)

            task.spawn(function()
                task.wait(1.5)
                local c = LocalPlayer.Character
                local hrp = c and c:FindFirstChild("HumanoidRootPart")
                if hrp then
                    local postDist = (hrp.Position - bestEntrance.Dest).Magnitude
                    if postDist > 150 and postDist >= preDist * 0.5 then
                        local fails = (_G.Fruit_TeleportFails[bestEntrance.Arg] or 0) + 1
                        _G.Fruit_TeleportFails[bestEntrance.Arg] = fails
                        if fails >= 3 then
                            _G.Fruit_BlockedEntrances[bestEntrance.Arg] = true
                        end
                    end
                end
            end)
            return
        end
    end

    local needSetup = not _G.Fruit_LinearVelocity or _G.Fruit_LinearVelocity.Parent ~= myHRP
    if needSetup then SetupFruitFlightComponents(myHRP) end

    if dir.Magnitude > 0.1 then
        _G.Fruit_LinearVelocity.VectorVelocity = dir.Unit * _G.FruitFlySpeed
    else
        _G.Fruit_LinearVelocity.VectorVelocity = Vector3.new(0, 0, 0)
    end

    local lookAt = Vector3.new(targetPos.X, myHRP.Position.Y, targetPos.Z)
    if (lookAt - myHRP.Position).Magnitude > 0.1 then
        myHRP.CFrame = CFrame.new(myHRP.Position, lookAt)
    end
end

local function StopFruitFlight()
    if _G.Fruit_LinearVelocity then
        _G.Fruit_LinearVelocity.VectorVelocity = Vector3.new(0, 0, 0)
    end
    ClearFruitFlightComponents()
    Fruit_OrbitOffset = nil
    Fruit_NextOrbitTime = 0

    local char = LocalPlayer.Character
    if char then
        SetCollide(char)
        local hum = char:FindFirstChildOfClass("Humanoid")
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if hum then hum:ChangeState(Enum.HumanoidStateType.Running) end
        if hrp then
            hrp.AssemblyLinearVelocity = Vector3.zero
            hrp.AssemblyAngularVelocity = Vector3.zero
        end
    end
end

local function GetActiveFruitCount()
    local count = 0
    for _, v in ipairs(workspace:GetChildren()) do
        if string.find(v.Name, "Fruit") and v:FindFirstChild("Handle") then
            count = count + 1
        end
    end
    return count
end

local function WaitForAllFruitsPicked(timeout)
    local startTime = tick()
    while GetActiveFruitCount() > 0 do
        if not _G.Tweenfruit then return false end
        if tick() - startTime >= timeout then return false end
        task.wait(1)
    end
    return true
end

local ServerCore = {}

function ServerCore:TeleportToServer(jobId)
    if not jobId or jobId == "" then
        _G.ServerHopping = false
        return
    end

    local now = tick()
    if (now - _G.LastTeleportTime) < _G.TELEPORT_COOLDOWN then
        task.wait(_G.TELEPORT_COOLDOWN - (now - _G.LastTeleportTime))
    end
    _G.LastTeleportTime = tick()
    _G.HopTargetJobId = jobId

    task.spawn(function()
        if ServerBrowserRemote then
            local success = pcall(function()
                ServerBrowserRemote:InvokeServer("teleport", jobId)
            end)
            if success then return end
        end

        local teleportOptions = Instance.new("TeleportOptions")
        teleportOptions.ServerInstanceId = jobId

        local ok = pcall(function()
            TeleportService:TeleportAsync(PlaceId, { LocalPlayer }, teleportOptions)
        end)
        if not ok then
            ok = pcall(function()
                TeleportService:TeleportToPlaceInstance(PlaceId, jobId, LocalPlayer)
            end)
        end
        if not ok then
            task.wait(5)
            _G.ServerHopping = false
            _G.HopTargetJobId = nil
        end
    end)

    if _G.HopConfirmConn then _G.HopConfirmConn:Disconnect(); _G.HopConfirmConn = nil end

    local hopStartTime = tick()
    local retryCount = 0
    local MAX_RETRIES = 5

    _G.HopConfirmConn = RunService.Heartbeat:Connect(function()
        if game.JobId ~= JobId then
            _G.HopConfirmConn:Disconnect(); _G.HopConfirmConn = nil
            _G.HopTargetJobId = nil
            return
        end
        if tick() - hopStartTime > 10 then
            retryCount = retryCount + 1
            if retryCount >= MAX_RETRIES then
                _G.HopConfirmConn:Disconnect(); _G.HopConfirmConn = nil
                _G.HopTargetJobId = nil
                _G.ServerHopping = false
                return
            end
            hopStartTime = tick()

            if GetActiveFruitCount() > 0 then
                _G.HopTargetJobId = nil
                _G.ServerHopping = false
                return
            end

            ServerCore:FindAndHop("random")
        end
    end)
end

function ServerCore:FindAndHop(mode)
    local url = "https://games.roblox.com/v1/games/" .. PlaceId .. "/servers/Public?sortOrder=Asc&limit=100"
    local success, response = pcall(function()
        return HttpService:JSONDecode(game:HttpGet(url))
    end)
    if not (success and response and response.data) then _G.ServerHopping = false; return end

    local validServers = {}
    for _, server in ipairs(response.data) do
        if server.id ~= JobId and server.id ~= _G.HopTargetJobId
            and server.playing and server.maxPlayers
            and server.playing < server.maxPlayers then
            table.insert(validServers, { id = server.id, count = server.playing })
        end
    end
    if #validServers == 0 then _G.ServerHopping = false; return end

    local targetJobId
    if mode == "small" then
        table.sort(validServers, function(a, b) return a.count < b.count end)
        targetJobId = validServers[1].id
    else
        targetJobId = validServers[math.random(1, #validServers)].id
    end
    self:TeleportToServer(targetJobId)
end

local function StartServerHopSequence()
    if _G.ServerHopping then return end
    _G.ServerHopping = true
    task.spawn(function()
        for i = _G.HopDelayTime, 1, -1 do
            if not _G.Tweenfruit then _G.ServerHopping = false; return end
            if GetActiveFruitCount() > 0 then
                _G.ServerHopping = false
                return
            end
            if WindUI then
                WindUI:Notify({ Title = "提示", Content = tostring(i) .. " 秒之後自動換服...", Duration = 1 })
            end
            task.wait(1)
        end
        if _G.Tweenfruit and GetActiveFruitCount() == 0 then
            ServerCore:FindAndHop("random")
        else
            _G.ServerHopping = false
        end
    end)
end

local function getClosestFruit()
    local char = LocalPlayer.Character
    local pHRP = char and char:FindFirstChild("HumanoidRootPart")
    if not pHRP then return nil end

    local closestFruit = nil
    local shortestDistance = math.huge

    for _, v in ipairs(workspace:GetChildren()) do
        if string.find(v.Name, "Fruit") and v:FindFirstChild("Handle") then
            local dist = (v.Handle.Position - pHRP.Position).Magnitude
            if dist < shortestDistance then
                shortestDistance = dist
                closestFruit = v.Handle
            end
        end
    end
    return closestFruit
end

task.spawn(function()
    -- 等待角色完全載入後才開始執行
    while not _G.ScriptReady do task.wait(1) end
    local jumpTimer = 0

    local function enterFlyMode(char)
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if hum then hum.PlatformStand = true; hum:ChangeState(Enum.HumanoidStateType.Physics) end
        if hrp then SetNoCollide(char) end
    end

    RunService.RenderStepped:Connect(function()
        if not _G.Tweenfruit then
            if _G.IsMovingToFruit then
                StopFruitFlight()
                _G.IsMovingToFruit = false
            end
            return
        end

        local char = LocalPlayer.Character
        if not char then return end

        local fruitHandle = getClosestFruit()

        if fruitHandle then
            _G.LastFruitTime = tick()
            _G.ForceHopTriggered = false
            _G.IsMovingToFruit = true

            enterFlyMode(char)

            local hrp = char:FindFirstChild("HumanoidRootPart")
            local dist = hrp and (hrp.Position - fruitHandle.Position).Magnitude or math.huge

            if dist <= 8 then
                if hrp then
                    hrp.CFrame = fruitHandle.CFrame + Vector3.new(0, 3, 0)
                end
                if _G.Fruit_LinearVelocity then
                    _G.Fruit_LinearVelocity.VectorVelocity = Vector3.new(0, 0, 0)
                end
                doJump()
            else
                FlyToFruitLogic(fruitHandle.Position)

                jumpTimer = jumpTimer + 1 / 60
                if jumpTimer >= 0.3 then
                    jumpTimer = 0
                    doJump()
                end
            end
        else
            if _G.IsMovingToFruit then
                StopFruitFlight()
                _G.IsMovingToFruit = false
                local hum = char:FindFirstChildOfClass("Humanoid")
                if hum then
                    hum.PlatformStand = false
                    hum:ChangeState(Enum.HumanoidStateType.Running)
                end
                SetCollide(char)
            end

            if not _G.ServerHopping then
                StartServerHopSequence()
            end
        end
    end)
end)

task.spawn(function()
    -- 等待角色完全載入後才開始執行
    while not _G.ScriptReady do task.wait(1) end
    while true do
        task.wait(10)
        if _G.Tweenfruit and not _G.ServerHopping then
            local fruitHandle = getClosestFruit()
            if fruitHandle then
                _G.LastFruitTime = tick()
                _G.ForceHopTriggered = false
            else
                if not _G.ForceHopTriggered and (tick() - _G.LastFruitTime) > 300 then
                    _G.ForceHopTriggered = true
                    StartServerHopSequence()
                end
            end
        else
            _G.LastFruitTime = tick()
            _G.ForceHopTriggered = false
        end
    end
end)

task.spawn(function()
    -- 等待角色完全載入後才開始執行
    while not _G.ScriptReady do task.wait(1) end
    local schedule = {
        { wait = 600, label = "10 分鐘定時換服", mode = "hop" },
        { wait = 300, label = "15 分鐘定時換服", mode = "hop" },
        { wait = 300, label = "20 分鐘定時換服", mode = "hop" },
        { wait = 600, label = "30 分鐘定時重連", mode = "rejoin" },
    }
    while true do
        for _, entry in ipairs(schedule) do
            task.wait(entry.wait)
            if entry.mode == "hop" then
                if GetActiveFruitCount() > 0 then
                    WaitForAllFruitsPicked(300)
                end
                _G.ServerHopping = false
                StopFruitFlight()
                ServerCore:FindAndHop("random")
            elseif entry.mode == "rejoin" then
                if GetActiveFruitCount() > 0 then
                    WaitForAllFruitsPicked(300)
                end
                task.wait(4)
                _G.ServerHopping = false
                StopFruitFlight()
                ServerCore:TeleportToServer(JobId)
            end
        end
    end
end)

--------------------------
-- 分頁
--------------------------
local Tabs = {
    Fruit = Window:Tab({ Title = "果實", Icon = "leaf" }),
    Settings = Window:Tab({ Title = "設定", Icon = "settings" })
}

--------------------------
-- 元件
--------------------------
Tabs.Fruit:Section({ Title = "Config" })

Tabs.Fruit:Dropdown({
    Title = "選擇陣營",
    Values = { "海賊", "海軍" },
    Value = _G.SelectTeam == "Pirates" and "海賊" or "海軍",
    Callback = function(v)
        _G.SelectTeam = v == "海賊" and "Pirates" or "Marines"
        SelectTeam()
        MarkConfigDirty()
    end,
})

Tabs.Fruit:Section({ Title = "果實" })

Tabs.Fruit:Toggle({ 
    Title = "ESP Fruit",
    Value = _G.FruitESP,
    Callback = function(v)
        _G.FruitESP = v
        if not _G.FruitESP then
            clearAllEsp()
        end
    end
})

Tabs.Fruit:Toggle({ 
    Title = "自動儲存果實",
    Value = _G.AutoStoreFruit,
    Callback = function(v)
        _G.AutoStoreFruit = v
    end
})

Tabs.Fruit:Toggle({ 
    Title = "自動抽果",
    Value = _G.AutoRollFruit,
    Callback = function(v)
        _G.AutoRollFruit = v
    end
})

Tabs.Fruit:Toggle({
    Title = "Auto Tween Fruit",
    Desc = "自動飛向果實撿取並自動換服",
    Value = _G.Tweenfruit,
    Callback = function(v)
        _G.Tweenfruit = v
    end
})

Tabs.Fruit:Slider({
    Title = "換服倒數 (秒)",
    Desc = "無果實時準備換服的等待時間",
    Value = {
    Min = 1,
    Max = 10,
    Default = _G.HopDelayTime,
    },
    Callback = function(v)
        _G.HopDelayTime = v
    end
})

Tabs.Settings:Section({ Title = "重置腳本" })

Tabs.Settings:Button({
    Title = "將所有配置恢復初始",
    Icon = "trash-2",
    Desc = "標記為待重置 下次啟動腳本時改為初始設定",
    Callback = function()
        local ok = pcall(function()
            if typeof(isfolder) == "function" and typeof(makefolder) == "function" and not isfolder(ConfigFolder) then
                makefolder(ConfigFolder)
            end
            if typeof(writefile) == "function" then
                writefile(ConfigFile, HttpService:JSONEncode(InitialConfig))
            end
        end)
        
        if ok then
            WindUI:Notify({
                Title = "配置等待重置",
                Content = "下次啟動腳本後將完全生效 當前功能不受影響",
                Duration = 4
            })
        else
            WindUI:Notify({
                Title = "重置失敗",
                Content = "無法儲存設定檔 請確認執行器是否支援檔案寫入",
                Duration = 4
            })
        end
    end
})

Tabs.Settings:Section({ Title = "設定" })

Tabs.Settings:Toggle({ 
    Title = "換服後自動執行",
    Desc = "切換伺服器時自動重新載入",
    Value = _G.AutoExecute,
    Callback = function(v)
        _G.AutoExecute = v
        
        if _G.AutoExecute then
            if queue_on_teleport then
                writefile("Hub.file", "true")
                queue_on_teleport(ScriptLoadstring)
                WindUI:Notify({
                    Title = "通知",
                    Content = "已開啟自動執行",
                    Duration = 3
                })
            else
                WindUI:Notify({
                    Title = "支援錯誤",
                    Content = "你目前的注入器不支援",
                    Duration = 3
                })
            end
        else
            if queue_on_teleport and isfile("Hub.file") then
                delfile("Hub.file")
            end
            WindUI:Notify({
                Title = "通知",
                Content = "已關閉自動執行",
                Duration = 3
            })
        end
    end
})

Tabs.Settings:Keybind({ 
    Title = "快捷鍵", 
    Desc = "可更改", 
    Value = _G.CurrentKey,
    Callback = function(v)
        _G.CurrentKey = v
        
        if not _G.KeyDisabled then
            Window:SetToggleKey(Enum.KeyCode[v])
        end
    end
})

Tabs.Settings:Toggle({
    Title = "停用快捷鍵",
    Desc = "開啟後將禁用快捷鍵",
    Value = _G.KeyDisabled,
    Callback = function(state)
        _G.KeyDisabled = state
        
        if state then
            Window:SetToggleKey(nil)
        else
            Window:SetToggleKey(Enum.KeyCode[_G.CurrentKey])
        end
    end
})

-- 初始化狀態
task.spawn(function()
    task.wait()

    -- 自動執行
    if _G.AutoExecute then
        if queue_on_teleport then
            if not isfile("Hub.file") then
                writefile("Hub.file", "true")
            end

            queue_on_teleport(ScriptLoadstring)
        end
    else
        if queue_on_teleport and isfile("Hub.file") then
            delfile("Hub.file")
        end
    end
end)
